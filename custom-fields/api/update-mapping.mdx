---
title: "Update Mapping"
description: "Update a custom field mapping. Only `json_path` is mutable — to change the scope of a mapping, delete it and create a new one."
openapi: patch /api/v1/custom-fields/mapping/{custom_field_mapping_id}
---
